import type { Reading as ReadingType, Aspect } from '@/lib/types';
import { mn } from '@/lib/mn';
export function Reading({reading}:{reading:ReadingType}){return <article className="reading-block"><p className="eyebrow">{reading.status==='approved'?'РЕДАКЦЫН ТАЙЛАЛ':'ТООЦООЛСОН БАРИМТ'}</p><h3>{reading.headline}</h3><p>{reading.summary}</p>{[['Давуу тал',reading.strengths],['Анхаарах зүйл',reading.challenges],['Харилцаа',reading.relationships]].map(([label,items])=>Array.isArray(items)&&items.length>0?<div key={String(label)}><h4>{String(label)}</h4><ul>{items.map(s=><li key={s}>{s}</li>)}</ul></div>:null)}<small className="muted">Эх сурвалж: {reading.source}</small></article>;}
export function AspectTable({aspects}:{aspects:Aspect[]}){return <div className="table-wrap"><table><thead><tr><th>Гаригууд</th><th>Холбоос</th><th>Өнцөг</th><th>Зөрүү</th></tr></thead><tbody>{aspects.map(a=><tr key={`${a.a}-${a.b}-${a.kind}`}><td>{mn.planets[a.a]} · {mn.planets[a.b]}</td><td>{mn.aspects[a.kind]}</td><td>{a.separation.toFixed(2)}°</td><td>{a.orb.toFixed(2)}°</td></tr>)}</tbody></table>{!aspects.length&&<p>Тогтоосон орбын хүрээнд гол холбоос олдсонгүй.</p>}</div>;}
